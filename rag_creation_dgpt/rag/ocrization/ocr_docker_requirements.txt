# Core dependencies - pinned for nougat-ocr 0.1.17 compatibility
nougat-ocr==0.1.17

# Google Cloud Storage for input/output
google-cloud-storage==2.10.0

# PyTorch and vision (pre-installed in base image, but constraining versions)
torch>=2.0.0,<2.2.0
torchvision>=0.15.0,<0.17.0

# Transformers and tokenization
transformers>=4.25.0,<4.36.0
sentencepiece>=0.1.99,<0.2.0

# Image processing - CRITICAL VERSION PINS
albumentations<1.4.0  # Must be <1.4.0 for ImageCompression API compatibility
opencv-python-headless>=4.7.0,<4.9.0

# PDF processing - CRITICAL VERSION PIN
pypdfium2==4.18.0  # Must be 4.18.0 for 'render' attribute compatibility

# Scientific computing
numpy<2  # nougat-ocr doesn't support numpy 2.x yet
Pillow>=9.0.0,<10.2.0

# Additional dependencies
tqdm>=4.62.0
timm==0.5.4  # MUST be 0.5.4 - required by nougat-ocr 0.1.17

# Retry logic
tenacity>=8.2.0